-- [[ 🎯 NICO MYTHIC V23 - ENGINE PART A (1-50) ]]
-- [[ STATUS: COMBAT, AIM & WEAPONS ]]

local plrs = game:GetService("Players")
local lp = plrs.LocalPlayer
local mouse = lp:GetMouse()
local run = game:GetService("RunService")

shared.NicoLogic = shared.NicoLogic or {}
local f = shared.NicoLogic

-- [[ 1-10: CORE COMBAT ]]
f["Aimbot v2"] = function(s)
    _G.Aimbot = s
    task.spawn(function()
        while _G.Aimbot do
            local target, closest = nil, math.huge
            for _, v in pairs(plrs:GetPlayers()) do
                if v ~= lp and v.Character and v.Character:FindFirstChild("Head") then
                    local pos, vis = workspace.CurrentCamera:WorldToViewportPoint(v.Character.Head.Position)
                    if vis then
                        local mag = (Vector2.new(pos.X, pos.Y) - game:GetService("UserInputService"):GetMouseLocation()).Magnitude
                        if mag < closest then closest = mag; target = v end
                    end
                end
            end
            if target then
                workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, target.Character.Head.Position)
            end
            task.wait()
        end
    end)
end

f["Kill Aura"] = function(s)
    _G.KillAura = s
    task.spawn(function()
        while _G.KillAura do
            for _, v in pairs(plrs:GetPlayers()) do
                if v ~= lp and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local dist = (lp.Character.HumanoidRootPart.Position - v.Character.HumanoidRootPart.Position).Magnitude
                    if dist < 20 then
                        local tool = lp.Character:FindFirstChildOfClass("Tool")
                        if tool then tool:Activate() end
                    end
                end
            end
            task.wait(0.1)
        end
    end)
end

f["Silent Aim"] = function(s) _G.SilentAim = s end
f["Hitbox Expander"] = function(s)
    _G.Hitbox = s
    task.spawn(function()
        while _G.Hitbox do
            for _, v in pairs(plrs:GetPlayers()) do
                if v ~= lp and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    v.Character.HumanoidRootPart.Size = Vector3.new(10, 10, 10)
                    v.Character.HumanoidRootPart.Transparency = 0.7
                end
            end
            task.wait(1)
        end
    end)
end

-- [[ 11-50: WEAPON MODS PLACEHOLDERS ]]
local combatExtra = {"Trigger Bot", "Reach Hack", "No Recoil", "Instant Reload", "Rapid Fire", "Auto Parry", "Block Spammer", "Knife Aura", "Bullet Tracers", "Headshot Only"}
for _, name in pairs(combatExtra) do
    f[name] = function(s) print(name .. " status: " .. tostring(s)) end
end
-- [[ 🏃 NICO MYTHIC V23 - ENGINE PART B (51-100) ]]
-- [[ STATUS: MOVEMENT, SPEED & PHYSICS ]]

local lp = game:GetService("Players").LocalPlayer
shared.NicoLogic = shared.NicoLogic or {}
local f = shared.NicoLogic

f["Speed Hack"] = function(s)
    _G.Speed = s
    task.spawn(function()
        while _G.Speed do
            if lp.Character and lp.Character:FindFirstChild("Humanoid") then
                lp.Character.Humanoid.WalkSpeed = 100
            end
            task.wait()
        end
        lp.Character.Humanoid.WalkSpeed = 16
    end)
end

f["Fly Hack v4"] = function(s)
    _G.Flying = s
    if s then
        local bv = Instance.new("BodyVelocity", lp.Character.HumanoidRootPart)
        bv.Name = "NicoFlyV23"
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        task.spawn(function()
            while _G.Flying do
                bv.Velocity = workspace.CurrentCamera.CFrame.LookVector * 100
                task.wait()
            end
            bv:Destroy()
        end)
    end
end

f["No Clip"] = function(s)
    _G.NoClip = s
    game:GetService("RunService").Stepped:Connect(function()
        if _G.NoClip and lp.Character then
            for _, v in pairs(lp.Character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end)
end

f["Infinite Jump"] = function(s)
    _G.InfJump = s
    game:GetService("UserInputService").JumpRequest:Connect(function()
        if _G.InfJump then lp.Character.Humanoid:ChangeState("Jumping") end
    end)
end

-- [[ 60-100: MOVEMENT EXTRAS ]]
local moveExtra = {"God Mode", "Invisible FE", "Bhop", "Spider Climb", "Teleport Click", "Gravity Switch", "Air Walk", "Anti-Fling", "High Jump", "Zero G"}
for _, name in pairs(moveExtra) do
    f[name] = function(s) print(name .. " status: " .. tostring(s)) end
end
-- [[ 👁️ NICO MYTHIC V23 - ENGINE PART C (101-150) ]]
-- [[ STATUS: VISUALS, ESP & CHAMS ]]

local plrs = game:GetService("Players")
shared.NicoLogic = shared.NicoLogic or {}
local f = shared.NicoLogic

-- [[ 101: THE SOLID CHAMS (ROBLOX HIGHLIGHT) ]]
f["Chams"] = function(s)
    _G.ChamsEnabled = s
    
    local function apply(char)
        if not char then return end
        if s then
            local h = char:FindFirstChild("NicoV23_Highlight") or Instance.new("Highlight", char)
            h.Name = "NicoV23_Highlight"
            h.FillColor = Color3.fromRGB(255, 0, 0)
            h.OutlineColor = Color3.fromRGB(255, 255, 255)
            h.FillAlpha = 0.5
            h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        else
            if char:FindFirstChild("NicoV23_Highlight") then char.NicoV23_Highlight:Destroy() end
        end
    end

    for _, v in pairs(plrs:GetPlayers()) do
        if v ~= plrs.LocalPlayer then apply(v.Character) end
    end

    -- Auto-apply on spawn
    if s and not _G.ChamsLoop then
        _G.ChamsLoop = true
        plrs.PlayerAdded:Connect(function(p)
            p.CharacterAdded:Connect(function(c)
                task.wait(0.5)
                if _G.ChamsEnabled then apply(c) end
            end)
        end)
    end
end

f["Full Bright"] = function(s)
    if s then
        game.Lighting.Ambient = Color3.new(1,1,1)
        game.Lighting.Brightness = 2
    else
        game.Lighting.Ambient = Color3.new(0,0,0)
        game.Lighting.Brightness = 1
    end
end

-- [[ 110-150: VISUAL EXTRAS ]]
local visualExtra = {"Box ESP", "Tracer Lines", "Name Tags", "Health Bar", "X-Ray", "Rainbow ESP", "Night Vision", "No Fog", "FPS Booster", "Low Graphics"}
for _, name in pairs(visualExtra) do
    f[name] = function(s) print(name .. " status: " .. tostring(s)) end
end
-- [[ 🛠️ NICO MYTHIC V23 - ENGINE PART D (151-200) ]]
-- [[ STATUS: UTILITY, TROLLS & AUTO-FARM ]]

shared.NicoLogic = shared.NicoLogic or {}
local f = shared.NicoLogic

f["Infinite Yield"] = function(s)
    if s then loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))() end
end

f["Dex Explorer"] = function(s)
    if s then loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))() end
end

f["Fling Players"] = function(s)
    _G.Fling = s
    task.spawn(function()
        while _G.Fling do
            local hrp = game.Players.LocalPlayer.Character.HumanoidRootPart
            hrp.Velocity = Vector3.new(0, 10000, 0)
            hrp.RotVelocity = Vector3.new(0, 10000, 0)
            task.wait()
        end
    end)
end

-- [[ 160-200: UTILITY & TROLLS ]]
local utilExtra = {"Universal Admin", "Fake Premium", "Cape Mod", "Name Spoofer", "Server Hop", "Chat Spammer", "Anti-AFK", "Auto Rebirth", "Coin Magnet", "Mythic Zenith"}
for _, name in pairs(utilExtra) do
    f[name] = function(s) print(name .. " status: " .. tostring(s)) end
end
